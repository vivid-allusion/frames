# Replicate Profile

# Project configuration
project: "WALTZ WITH BASHIR"

paths:
  input: "/Users/ruben/Nextcloud/01 - PROJECTS/251230_WBT/02_GENERATIONS/@INPUT(img-to-img)"
  output: "/Users/ruben/Nextcloud/01 - PROJECTS/251230_WBT/02_GENERATIONS/"

  # Model endpoint (required)
endpoint: "google/nano-banana-pro"

# Pricing information (optional, for cost tracking)
pricing:
  base_cost: 0.14
  currency: "USD"

# Model parameters (required) - all parameters passed through to API
parameters:
  num_images: 1
  aspect_ratio: "16:9"
  output_format: "png"
  resolution: "2K"
  safety_filter_level: "block_only_high"

# image_input: "https://f003.backblazeb2.com/file/wbt-bucket/references/ari_foldman_young.png"

# Processing options (optional)
delay_between_requests: 1.0

# Fixes
prompt_prefix: 'Create an photorealistic version of the provided drawing. The drawing is suppose to depict a scene from 1982 civil war in Lebanon war. The drawing shows '
prompt_suffix: 'The image you create should look like it was shot on 35mm film by Roger Deakins.'
