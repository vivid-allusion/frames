wrapper:
  name: "Text-to-Image Generator"
  version: "2.0.0"

tool:
  name: "Text-to-Image Generator"
  version: "2.0.0"

processing:
  max_workers: 1
  batch_size: 10

api:
  timeout: 120
  max_retries: 3
  max_wait_time: 600

output:
  force_png: false
  create_metadata: true

logging:
  level: "INFO"
  format: "simple"