# Replicate Profile

# Project configuration
project: "David"

paths:
  input: "/Users/ruben/Nextcloud/01 - PROJECTS/260221_DAVID/02_DAVID_GEN_IMG/@INPUT(img-to-img)"
  output: "/Users/ruben/Nextcloud/01 - PROJECTS/260221_DAVID/02_DAVID_GEN_IMG/"
  # Model endpoint (required)
endpoint: "google/nano-banana-2"

# Pricing information (optional, for cost tracking)
pricing:
  base_cost: 0.067
  currency: "USD"

# Model parameters (required) - all parameters passed through to API
parameters:
  num_images: 1
  aspect_ratio: "21:9"
  output_format: "png"
  resolution: "1K"
  safety_filter_level: "block_only_high"

# image_input: "https://f003.backblazeb2.com/file/wbt-bucket/references/ari_foldman_young.png"

# Processing options (optional)
delay_between_requests: 1.0

# Fixes
prompt_prefix: 'Create an exact, photorealistic version of the provided hand-drawing. The drawing is suppose to depict a scene from the Kingdom of David ca 1000 BC. The drawing is made in rough style take liberties to render characters and objects correctly. Like the color style of the drawing, the shadows should be deep, and the colors. Deep chiaroscuro lighting with near-black shadows, hyper-saturated primary colors (crimson, teal, gold), extreme contrast ratios between lit and unlit areas, and theatrical stage-like illumination that treats darkness as a compositional element rather than absence of light. The drawing shows '
prompt_suffix: 'The created image should look like it was shot on 35mm film by Roger Deakins.'
